// Scaffold smoke test — confirms workspace compiles.
// Real integration tests (sweettest cross-DID proof round-trip,
// link-resistance, unlinkability) land under tasks #14.

#[test]
fn workspace_compiles() {
    // Intentionally trivial.
}
