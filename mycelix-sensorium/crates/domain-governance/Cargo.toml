[package]
name = "domain-governance"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Governance domain module for Mycelix Sensorium — proposals, voting, councils"

[dependencies]
sensorium-domain-trait = { path = "../sensorium-domain-trait" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
rmp-serde = "1"
