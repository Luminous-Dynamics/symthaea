[package]
name = "domain-commons"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Commons domain module for Mycelix Sensorium — property, housing, care, mutual aid, water, food, transport"

[dependencies]
sensorium-domain-trait = { path = "../sensorium-domain-trait" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
rmp-serde = "1"
