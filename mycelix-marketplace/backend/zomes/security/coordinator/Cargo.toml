[package]
name = "security_coordinator"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]
name = "security_coordinator"

[dependencies]
hdk.workspace = true
serde.workspace = true
thiserror.workspace = true

# Link to integrity zome
security_integrity = { path = "../integrity" }
