[package]
name = "domain-finance"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Finance domain module for Mycelix Portal — payments, treasury, staking, recognition"

[dependencies]
portal-domain-trait = { path = "../portal-domain-trait" }
serde = { version = "1", features = ["derive"] }
