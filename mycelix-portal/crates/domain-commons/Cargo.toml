[package]
name = "domain-commons"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Commons domain module for Mycelix Portal — property, housing, care, mutual aid, water, food, transport"

[dependencies]
portal-domain-trait = { path = "../portal-domain-trait" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
rmp-serde = "1"
