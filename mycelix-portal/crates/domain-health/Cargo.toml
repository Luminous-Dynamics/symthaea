[package]
name = "domain-health"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Health domain module for Mycelix Portal"

[dependencies]
portal-domain-trait = { path = "../portal-domain-trait" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
rmp-serde = "1"
