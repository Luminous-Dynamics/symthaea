[package]
name = "portal-domain-trait"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Domain module trait for Mycelix Portal — the contract every cluster implements"

[dependencies]
