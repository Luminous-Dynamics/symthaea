[package]
name = "domain-governance"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Governance domain module for Mycelix Portal — proposals, voting, councils"

[dependencies]
portal-domain-trait = { path = "../portal-domain-trait" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
rmp-serde = "1"
