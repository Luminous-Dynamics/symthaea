[package]
name = "byzantine_fl"
version = "0.1.0"
edition = "2021"

[dependencies]
hdk = "0.6.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib", "rlib"]