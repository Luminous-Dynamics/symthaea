# Grafana dashboard provisioning

apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: 'Proof Service'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
