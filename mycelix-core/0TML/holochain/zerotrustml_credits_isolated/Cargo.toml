[package]
name = "zerotrustml_credits_isolated"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

# Empty workspace to prevent parent workspace detection
[workspace]

[dependencies]
# Exact versions per Holochain 0.4 upgrade guide
hdk = "=0.4.0"
hdi = "=0.5.0"
serde = "1.0"