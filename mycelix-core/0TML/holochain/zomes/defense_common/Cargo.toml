[package]
name = "defense_common"
version = "0.1.0"
edition = "2021"
description = "Common utilities for Byzantine defense zomes: error handling, logging, metrics"
authors = ["Luminous Dynamics Research Team"]
license = "Apache-2.0"

[workspace]

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

# HDK for Holochain types (optional, for AgentPubKey)
hdk = { version = "0.4", optional = true }

[features]
default = []
holochain = ["hdk"]
