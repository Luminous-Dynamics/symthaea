# 0TML Notes Archive

High-volume status logs and milestone documents that previously cluttered the
project root are collected here. Organize further by creating descriptive
subdirectories (`status/`, `planning/`, `reports/`, etc.) as you continue the
cleanup.
