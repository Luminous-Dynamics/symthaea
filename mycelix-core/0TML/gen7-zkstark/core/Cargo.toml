[package]
name = "gradient-proof-core"
version = "0.1.0"
edition = "2021"
description = "Shared types and utilities for gradient validity proofs"

[features]
default = ["std"]
std = ["serde/std"]

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive"] }
