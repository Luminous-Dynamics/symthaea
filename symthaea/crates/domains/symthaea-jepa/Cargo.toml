[package]
name = "symthaea-jepa"
version = "0.1.0"
edition = "2024"
description = "JEPA: Joint Embedding Predictive Architecture for thermodynamically efficient latent-space prediction in HDC"
license = "AGPL-3.0-or-later"

[dependencies]
symthaea-core = { path = "../../core/symthaea-core" }
serde = { workspace = true }
rand = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
