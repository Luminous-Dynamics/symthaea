# Verification Matrix Alpha.10

The verification matrix is an operator-facing checklist. It records which checks are smoke-level, local-research-level, pilot-level, or external/future-only.

It is not a certificate.

Useful command:

`cargo run --bin symthaea-quantum-comp -- verify-matrix`

The matrix explicitly keeps external quantum backend observation and peer review outside local crate authority.
