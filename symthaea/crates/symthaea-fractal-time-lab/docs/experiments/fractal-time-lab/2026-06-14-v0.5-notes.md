# Fractal Time Lab v0.5 Notes

Date: 2026-06-14  
Status: exploratory benchmark hardening

## Added

- `BenchmarkRun` with explicit claim/non-claim boundaries.
- `report.rs` Markdown report generation.
- `--markdown`, `--markdown-out`, `--json-run`, and `--json-run-out`.
- `--fail-on-benchmark-fail` for optional CI gating.
- Cycle graph null model.
- Stronger tests for claim scope and Markdown serialization.

## Verification checklist

Record exact outputs after running:

```bash
cargo test -p symthaea-fractal-time-lab --lib
cargo test -p symthaea-fractal-time-lab --test benchmark_sanity
cargo run -p symthaea-fractal-time-lab --example run_experiments -- --seed 42 --trials 32
cargo run -p symthaea-fractal-time-lab --example run_experiments -- --json-run --seed 42 --trials 32
cargo run -p symthaea-fractal-time-lab --example run_experiments -- --markdown --seed 42 --trials 32
```

## Non-claims

This milestone does not claim:

- proof of fractal time,
- proof of quantum consciousness,
- physical time-crystal simulation,
- full IIT Phi,
- production scientific validation.

Honest claim:

> `symthaea-fractal-time-lab` is an exploratory benchmark crate for testing structural hypotheses against explicit null models and producing scoped evidence reports.
