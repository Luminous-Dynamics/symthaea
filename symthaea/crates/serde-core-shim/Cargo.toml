[package]
name = "serde_core"
version = "1.0.228"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Local shim — re-exports serde to bridge the serde_core split"

[dependencies]
serde = "1.0"
