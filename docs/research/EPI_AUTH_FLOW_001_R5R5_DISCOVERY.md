# EPI-AUTH-FLOW-001 R5R5 — bypass-status → ordinal/cube/prompt expression discovery

## Purpose

R5R5 is a fresh measurement-only replacement for superseded R5R4.

R5R4 correctly added the independent per-axis `EpistemicCubeGate`, but deeper preflight exposed two remaining authority-flow gaps:

1. `Probable` shares the 1D Broca gate bypass with `Certain` because the ordinal gate returns without modification for any ordinal `< 1.5` (`Certain=0`, `Probable=1`). Multiple internal heuristics can mint `Probable`, including Phi/meta-awareness, HDC familiarity/resonance, and coding-agent confidence.
2. epistemic authority also reaches expression through prompt conditioning: `StructuredThought` emits `EPISTEMIC_STATUS`, while the translation system prompt instructs the LLM to speak confidently for `Certain`, treats legacy E4/N3/M3 as proof/axiomatic/foundational semantics, and currently says deterministic Rust `COMPUTED_ANSWER` values are guaranteed correct.

R5R5 therefore inventories the complete bounded chain before any semantic repair is admitted.

## Core theorem

```text
internal confidence / familiarity / Phi / meta-awareness
!= proposition-specific admitted authority
```

```text
Certain OR Probable
!= permission to bypass an epistemic generation safeguard
```

```text
deterministic computation
!= correctness proof
```

```text
legacy E/N/M coordinate
!= permission to strengthen assertion
!= permission to suppress hedging
!= axiomatic/foundational expression authority
```

## Discovery surfaces

The executable emits deterministic sorted production-Rust path sets and SHA-256 path-set digests for:

- typed E3/E4 and N3/M3 authority-shaped values;
- typed epistemic-cube definitions;
- `EpistemicStatus` definitions;
- both ordinal-bypass status variants (`Certain`, `Probable`);
- `EpistemicCode::{Confident,Probable}` conversion surfaces;
- ordinal propagation;
- Broca 1D ordinal gating;
- Broca 4D cube gating;
- strict-code epistemic gating;
- explicit generator/backend gate controls including direct `bypass_gating`, per-gate enables, Liquid-Mamba `enable_gating`, and familiarity attenuation;
- LLM prompt authority/status/cube/computed-answer surfaces;
- legacy E3/E4/N3/M3 aliases;
- raw numeric cube transport and scalar collapse;
- Broca cube sinks.

Standalone `examples/`, `tests/`, and `benches/` are excluded from discovery. Production `src/bin` surfaces remain visible.

## Required producer witnesses

R5R5 fails if the exact source no longer demonstrates representative current producers for:

```text
cognitive-loop confidence / knowledge grounding
  -> numeric E/N/M cube
```

```text
StructuredThought ETier
  -> Certain / Probable
```

```text
Phi + meta-awareness / HDC familiarity-resonance
  -> Certain / Probable
```

```text
coding-agent confidence / EpistemicCode
  -> Certain / Probable
```

These are inventoried as current source behavior, not endorsed as valid epistemic authority.

## Required expression-consumer witnesses

R5R5 also freezes representative current paths for:

```text
Certain / Probable
  -> ordinal 0 / 1
  -> Broca 1D gate no-modification branch
```

```text
E/N/M cube
  -> EpistemicCubeGate
  -> assertion / hedging / axiomatic / foundational token modulation
```

```text
E tier > 1
  -> strict-code high-certainty early return
```

```text
StructuredThought epistemic status/cube/computed answer
  -> LLM prompt
  -> expression instructions
```

and backend-specific controls:

```text
BrocaGenerator:
  bypass_gating + enable_epistemic_gate + enable_epistemic_cube_gate

LiquidMambaGenerator:
  enable_gating -> cube gate + ordinal gate

Spore Broca:
  enable_epistemic_gate -> familiarity-aware ordinal gate
```

## Prompt authority boundary

The current translation prompt is an authority-bearing consumer because it contains directives such as:

```text
Certain -> speak confidently
Probable -> likely/probably
E4 -> reproducible proof
N3 -> axiomatic truth
M3 -> foundational
COMPUTED_ANSWER -> guaranteed correct
```

R5R5 records that behavior only.

The successor repair must instead preserve distinctions such as:

```text
computed deterministically
!= verified correct

legacy E4
!= admitted reproduction evidence

legacy N3
!= admitted axiomatic/formal basis

legacy M3
!= admitted foundational scope
```

## Qualification semantics

An exact-head qualifier may claim only:

```text
schema=epi-auth-flow-001-r5r5-discovery-v1
authority_scope=measurement-only-producer-bypass-status-ordinal-cube-prompt-gates-and-controls
result=PASS_DISCOVERY
```

`PASS_DISCOVERY` means that the exact frozen discovery program executed successfully against the exact frozen product and emitted its complete observed surface.

It is not a frozen inventory and does not establish semantic correctness.

## R6 handoff

Only after exact R5R5 executable success should R6 be constructed mechanically as a fresh direct child of unchanged `main`.

R6 should freeze, for every surface:

- exact ordered path list;
- path-set digest;
- exact file/blob identity for every listed path;
- required witness contract;
- exact R5R5 discovery subject/run identity used to derive the frozen set.

R6's success state remains `PASS_INVENTORY`, not a scientific/semantic PASS.

## Base

Prepared only as a fresh direct child of:

`adb69f11fa8068b019cc5bb598d0c7726a197fc9`

base tree:

`35a6c5fdba319556af9bb487838734f67c8ac0d6`

## Nonclaims

R5R5 does not establish cryptographic verification, public reproducibility, consensus, axiomatic truth, foundational status, computation correctness, causal validity, scientific truth, action authority, direct-expression authority, hedging-suppression authority, or gate-bypass authority.
