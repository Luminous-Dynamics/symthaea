# LQCD-020P authority boundary

The fit-family API intentionally returns all declared model fits. It has no model selector and no `best_model` field.

Any later statement that one model/range is preferred must come from a separately preregistered analysis plan and evidence contract. This prevents post-hoc ansatz selection from becoming implicit authority inside the numerical primitive.
