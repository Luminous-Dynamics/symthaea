[package]
name = "mycelix_finance_shared"
version = "0.1.0"
edition = "2021"
description = "Shared utilities for Mycelix Finance zomes - batch queries, validation, and common types"

[lib]
crate-type = ["cdylib", "rlib"]
name = "mycelix_finance_shared"

[dependencies]
hdk.workspace = true
serde = { workspace = true }
serde_json = { workspace = true }
mycelix_finance_types = { path = "../../types" }

[dev-dependencies]

[lints.rust]
unsafe_code = "forbid"
