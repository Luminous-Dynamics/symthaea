[package]
name = "recognition_integrity"
version = "0.1.0"
edition = "2021"
description = "Recognition integrity zome — MYCEL reputation via weighted recognition"

[lib]
crate-type = ["cdylib", "rlib"]
name = "recognition_integrity"

[dependencies]
holochain_serialized_bytes = { workspace = true }
hdi = { workspace = true }
serde = { workspace = true }
mycelix_finance_types = { path = "../../../types" }
