[package]
name = "staking_integrity"
version = "0.1.0"
edition = "2021"
description = "Tri-asset staking integrity zome with cryptographic proofs"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdi = { workspace = true }
holochain_serialized_bytes = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
mycelix_finance_types = { path = "../../../types" }

[features]
default = []
