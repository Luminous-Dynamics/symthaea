[package]
name = "finance-wire-types"
version = "0.1.0"
edition = "2021"
description = "Wire types for Mycelix Finance — external consumer types for cross-cluster calls"
license = "AGPL-3.0-or-later"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
