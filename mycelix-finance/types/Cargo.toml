[package]
name = "mycelix_finance_types"
version = "0.1.0"
edition = "2021"
description = "Shared economic type definitions for Mycelix Finance — no HDK/HDI dependency"

[lib]
crate-type = ["rlib"]
name = "mycelix_finance_types"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
