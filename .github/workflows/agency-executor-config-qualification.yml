name: Agency Executor Configuration Qualification

on:
  pull_request:
    paths:
      - 'crates/bridges/symthaea-executor-configuration/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'
      - '.github/workflows/agency-executor-config-qualification.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: agency-executor-config-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  executor-config:
    name: Executor configuration source qualification
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install pinned Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: clippy,rustfmt

      - name: Preserve checked-in lockfile
        run: cp Cargo.lock "$RUNNER_TEMP/Cargo.lock.before"

      - name: Ask Cargo to reconcile workspace metadata
        run: cargo metadata --format-version 1 >/dev/null

      - name: Prove lock reconciliation is additive workspace metadata only
        run: |
          python3 - <<'PY'
          import os, pathlib, sys, tomllib
          before = tomllib.loads((pathlib.Path(os.environ["RUNNER_TEMP"]) / "Cargo.lock.before").read_text())
          after = tomllib.loads(pathlib.Path("Cargo.lock").read_text())
          key = lambda p: (p["name"], p["version"], p.get("source"))
          b = {key(p): p for p in before.get("package", [])}
          a = {key(p): p for p in after.get("package", [])}
          removed = sorted(set(b) - set(a), key=repr)
          changed = sorted((k for k in b.keys() & a if b[k] != a[k]), key=repr)
          added = sorted(set(a) - set(b), key=repr)
          sourced = [k for k in added if k[2] is not None]
          if removed or changed or sourced:
              print("Cargo reconciliation changed sourced dependency state")
              print("removed:", removed)
              print("changed:", changed)
              print("new sourced:", sourced)
              sys.exit(1)
          print("additive workspace/path nodes:", len(added))
          for item in added:
              print(" +", item)
          PY

      - name: Record lock freshness
        run: |
          if git diff --quiet -- Cargo.lock; then
            echo clean > "$RUNNER_TEMP/agency-lock-status"
          else
            echo stale > "$RUNNER_TEMP/agency-lock-status"
            git --no-pager diff -- Cargo.lock
          fi

      - name: Upload Cargo-produced lock candidate
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agency-executor-config-cargo-lock-${{ github.sha }}
          path: Cargo.lock
          if-no-files-found: error
          retention-days: 7

      - name: Rustfmt executor configuration crate
        run: cargo fmt -p symthaea-executor-configuration -- --check

      - name: Test executor configuration parser and policy
        run: cargo test --locked -p symthaea-executor-configuration

      - name: Clippy executor configuration crate
        run: cargo clippy --locked -p symthaea-executor-configuration --all-targets -- -D warnings

      - name: Record exact qualification evidence
        if: always()
        run: |
          mkdir -p "$RUNNER_TEMP/agency-executor-config-evidence"
          git rev-parse HEAD > "$RUNNER_TEMP/agency-executor-config-evidence/HEAD"
          git rev-parse HEAD^{tree} > "$RUNNER_TEMP/agency-executor-config-evidence/TREE"
          rustc -Vv > "$RUNNER_TEMP/agency-executor-config-evidence/RUSTC.txt"
          cargo -V > "$RUNNER_TEMP/agency-executor-config-evidence/CARGO.txt"
          sha256sum Cargo.lock > "$RUNNER_TEMP/agency-executor-config-evidence/CARGO_LOCK_SHA256.txt"
          cp Cargo.lock "$RUNNER_TEMP/agency-executor-config-evidence/Cargo.lock"

      - name: Upload qualification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agency-executor-config-evidence-${{ github.sha }}
          path: ${{ runner.temp }}/agency-executor-config-evidence
          if-no-files-found: error
          retention-days: 14

      - name: Require checked-in Cargo.lock to be fresh
        run: test "$(cat "$RUNNER_TEMP/agency-lock-status")" = clean
