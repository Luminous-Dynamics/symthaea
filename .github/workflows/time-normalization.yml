name: Time Normalization

on:
  push:
    branches: [main]
    paths:
      - "crates/core/symthaea-time-integrity/**"
      - "crates/core/symthaea-time-normalization/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/time-normalization.yml"
  pull_request:
    paths:
      - "crates/core/symthaea-time-integrity/**"
      - "crates/core/symthaea-time-normalization/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/time-normalization.yml"
  workflow_dispatch:

concurrency:
  group: time-normalization-${{ github.ref }}-${{ github.event_name == 'workflow_dispatch' && github.run_id || 'auto' }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Time normalization unit and lint gates
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: clippy

      - name: Cache cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-time-normalization-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-time-normalization-

      - name: Run time normalization unit tests
        run: cargo test -p symthaea-time-normalization

      - name: Run time normalization clippy
        run: cargo clippy -p symthaea-time-normalization --all-targets -- -D warnings

      - name: Run time normalization documentation tests
        run: cargo test -p symthaea-time-normalization --doc
