name: Extension Simulation Host

on:
  pull_request:
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/core/symthaea-extension-core/**"
      - "crates/bridges/symthaea-extension-host/**"
      - "crates/bridges/symthaea-extension-simulation-host/**"
      - "crates/bridges/symthaea-sim-bridge/**"
      - ".github/workflows/extension-simulation-host.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTC_WRAPPER: ""
  SCCACHE_DISABLE: "1"

jobs:
  simulation-host:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-extension-simulation-host-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-extension-simulation-host-

      - name: Reconcile candidate lockfile
        run: |
          cargo metadata --format-version 1 > /dev/null
          git diff -- Cargo.lock > extension-simulation-host-cargo-lock.patch
          if git diff --quiet -- Cargo.lock; then
            echo "Cargo.lock already matches the simulation host workspace graph."
          else
            echo "Cargo.lock requires the following generated update:"
            cat extension-simulation-host-cargo-lock.patch
          fi

      - name: Upload candidate Cargo.lock patch
        uses: actions/upload-artifact@v4
        with:
          name: extension-simulation-host-cargo-lock-patch
          path: extension-simulation-host-cargo-lock.patch
          if-no-files-found: error

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Check simulation host
        run: |
          cargo check --locked -p symthaea-extension-host --all-targets
          cargo check --locked -p symthaea-extension-simulation-host --all-targets

      - name: Test simulation host
        run: |
          cargo test --locked -p symthaea-extension-host
          cargo test --locked -p symthaea-extension-simulation-host

      - name: Strict Clippy
        run: |
          cargo clippy --locked -p symthaea-extension-host --all-targets -- -D warnings
          cargo clippy --locked -p symthaea-extension-simulation-host --all-targets -- -D warnings

      - name: Require committed lockfile
        run: git diff --exit-code -- Cargo.lock
