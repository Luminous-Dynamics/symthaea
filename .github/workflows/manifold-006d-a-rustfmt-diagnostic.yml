name: MANIFOLD-006D-A Rustfmt Diagnostic

on:
  workflow_dispatch:

permissions: {contents: read}

jobs:
  superseded:
    runs-on: ubuntu-slim
    steps:
      - name: Preserve superseded diagnostic lineage
        run: echo 'The exact-formatter experiment was superseded before interpretation. Replacement A self-verifies formatting under Rust 1.96.'
