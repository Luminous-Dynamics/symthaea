name: MANIFOLD-006D-A Rustfmt Diagnostic

on:
  workflow_dispatch:

permissions: {contents: read}

jobs:
  superseded:
    runs-on: ubuntu-slim
    steps:
      - run: echo 'Superseded by the replacement A qualification lane; preserved branch history remains diagnostic evidence.'
