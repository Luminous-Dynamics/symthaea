name: Time Integrity

on:
  push:
    branches: [main]
    paths:
      - "crates/core/symthaea-time-integrity/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/time-integrity.yml"
  pull_request:
    paths:
      - "crates/core/symthaea-time-integrity/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/time-integrity.yml"
  workflow_dispatch:

concurrency:
  group: time-integrity-${{ github.ref }}-${{ github.event_name == 'workflow_dispatch' && github.run_id || 'auto' }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Time integrity unit and lint gates
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: clippy

      - name: Cache cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-time-integrity-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-time-integrity-

      - name: Run time-integrity unit tests
        run: cargo test -p symthaea-time-integrity

      - name: Run time-integrity clippy
        run: cargo clippy -p symthaea-time-integrity --all-targets -- -D warnings

      - name: Run time-integrity documentation tests
        run: cargo test -p symthaea-time-integrity --doc
