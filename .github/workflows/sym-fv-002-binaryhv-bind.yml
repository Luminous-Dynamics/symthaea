name: SYM-FV-002 BinaryHV Bind

on:
  pull_request:
    paths:
      - "formal/lean/hdc/BinaryHVBind.lean"
      - "docs/formal/sym-fv-002-binaryhv-bind-v1.json"
      - "scripts/validate_sym_fv_002.py"
      - "crates/core/symthaea-core/src/hdc/binary_hv.rs"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/sym-fv-002-binaryhv-bind.yml"
  push:
    branches: [main]
    paths:
      - "formal/lean/hdc/BinaryHVBind.lean"
      - "docs/formal/sym-fv-002-binaryhv-bind-v1.json"
      - "scripts/validate_sym_fv_002.py"
      - "crates/core/symthaea-core/src/hdc/binary_hv.rs"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/sym-fv-002-binaryhv-bind.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sym-fv-002-${{ github.event.pull_request.head.sha || github.sha }}
  cancel-in-progress: false

jobs:
  binaryhv-bind:
    name: Lean abstract bind theorem
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout exact subject
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Record exact Git subject
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha || github.sha }}"
          git rev-parse HEAD
          git rev-parse HEAD^{tree}
          git status --porcelain=v1 --untracked-files=all

      - name: Static contract and negative controls
        run: python3 scripts/validate_sym_fv_002.py

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Prove the wrapper propagates Lean failure
        run: |
          set -euo pipefail
          cat > /tmp/sym-fv-002-known-bad.lean <<'LEAN'
          example : False := by
            trivial
          LEAN
          if nix develop .#formal --command sh -c 'lean /tmp/sym-fv-002-known-bad.lean > /tmp/sym-fv-002-known-bad-output.txt 2>&1'; then
            cat /tmp/sym-fv-002-known-bad-output.txt
            echo "known-bad Lean subject incorrectly returned success" >&2
            exit 1
          fi
          cat /tmp/sym-fv-002-known-bad-output.txt
          grep -q 'error:' /tmp/sym-fv-002-known-bad-output.txt

      - name: Typecheck Lean theorem subject fail-closed
        run: |
          set -euo pipefail
          nix develop .#formal --command sh -c '
            set -eu
            lean --version > /tmp/sym-fv-002-lean-version.txt
            cat /tmp/sym-fv-002-lean-version.txt
            if ! lean formal/lean/hdc/BinaryHVBind.lean > /tmp/sym-fv-002-lean-output.txt 2>&1; then
              cat /tmp/sym-fv-002-lean-output.txt
              exit 1
            fi
            cat /tmp/sym-fv-002-lean-output.txt
            if grep -q "sorryAx" /tmp/sym-fv-002-lean-output.txt; then
              echo "forbidden sorryAx in Lean axiom census" >&2
              exit 1
            fi
            if grep -q "error:" /tmp/sym-fv-002-lean-output.txt; then
              echo "Lean output contains an error marker despite zero exit" >&2
              exit 1
            fi
          '

      - name: Postflight immutability and receipt
        run: |
          set -euo pipefail
          git diff --exit-code
          test -z "$(git status --porcelain=v1 --untracked-files=all)"
          python3 - <<'PY'
          import hashlib
          import json
          import subprocess
          from pathlib import Path

          root = Path.cwd()
          source_commit = "458c7b98d81c64b9361e252f85ef9d45132e6682"
          source_path = "crates/core/symthaea-core/src/hdc/binary_hv.rs"
          paths = {
              "proof": root / "formal/lean/hdc/BinaryHVBind.lean",
              "manifest": root / "docs/formal/sym-fv-002-binaryhv-bind-v1.json",
              "validator": root / "scripts/validate_sym_fv_002.py",
              "flake_lock": root / "flake.lock",
              "lean_output": Path("/tmp/sym-fv-002-lean-output.txt"),
              "known_bad_output": Path("/tmp/sym-fv-002-known-bad-output.txt"),
          }

          def git(*args):
              return subprocess.check_output(["git", *args], text=True).strip()

          def sha256(path):
              return hashlib.sha256(path.read_bytes()).hexdigest()

          receipt = {
              "schema": "symthaea.formal.sym-fv-002-qualification-receipt.v2",
              "evidence_class": "AbstractFormalTheorem",
              "result": "LeanTypecheckedAndAxiomClean",
              "head": git("rev-parse", "HEAD"),
              "tree": git("rev-parse", "HEAD^{tree}"),
              "source_commit": source_commit,
              "source_path": source_path,
              "source_blob": git("rev-parse", f"{source_commit}:{source_path}"),
              "head_source_blob": git("rev-parse", f"HEAD:{source_path}"),
              "lean_version": Path("/tmp/sym-fv-002-lean-version.txt").read_text().strip(),
              "proof_sha256": sha256(paths["proof"]),
              "manifest_sha256": sha256(paths["manifest"]),
              "validator_sha256": sha256(paths["validator"]),
              "flake_lock_blob": git("rev-parse", "HEAD:flake.lock"),
              "flake_lock_sha256": sha256(paths["flake_lock"]),
              "lean_output_sha256": sha256(paths["lean_output"]),
              "known_bad_output_sha256": sha256(paths["known_bad_output"]),
              "static_contract": "PASS",
              "negative_controls": "PASS",
              "exact_head_checkout": True,
              "known_bad_lean_subject_rejected": True,
              "lean_exit_zero": True,
              "sorryAx_absent": True,
              "lean_error_markers_absent": True,
              "repository_postflight_clean": True,
              "nonclaims": [
                  "not-rust-refinement",
                  "not-simd-proof",
                  "not-compiler-or-native-binary-proof",
                  "not-aeneas-extraction",
              ],
          }
          Path("/tmp/sym-fv-002-receipt.json").write_text(
              json.dumps(receipt, indent=2, sort_keys=True) + "\n", encoding="utf-8"
          )
          print(json.dumps(receipt, indent=2, sort_keys=True))
          PY

      - name: Retain proof evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: sym-fv-002-${{ github.event.pull_request.head.sha || github.sha }}
          path: |
            /tmp/sym-fv-002-lean-version.txt
            /tmp/sym-fv-002-lean-output.txt
            /tmp/sym-fv-002-known-bad-output.txt
            /tmp/sym-fv-002-receipt.json
          if-no-files-found: error
          retention-days: 30
