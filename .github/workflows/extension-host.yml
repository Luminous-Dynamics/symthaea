name: Extension Host

on:
  pull_request:
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/core/symthaea-extension-core/**"
      - "crates/bridges/symthaea-extension-host/**"
      - ".github/workflows/extension-host.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTC_WRAPPER: ""
  SCCACHE_DISABLE: "1"

jobs:
  extension-host:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-extension-host-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-extension-host-

      - name: Reconcile candidate lockfile
        run: |
          cargo metadata --format-version 1 > /dev/null
          git diff -- Cargo.lock > extension-host-cargo-lock.patch
          if git diff --quiet -- Cargo.lock; then
            echo "Cargo.lock already matches the extension host graph."
          else
            echo "Cargo.lock requires the following generated update:"
            cat extension-host-cargo-lock.patch
          fi

      - name: Upload candidate Cargo.lock patch
        uses: actions/upload-artifact@v4
        with:
          name: extension-host-cargo-lock-patch
          path: extension-host-cargo-lock.patch
          if-no-files-found: error

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Check host
        run: cargo check --locked -p symthaea-extension-host --all-targets

      - name: Test host
        run: cargo test --locked -p symthaea-extension-host

      - name: Clippy host
        run: cargo clippy --locked -p symthaea-extension-host --all-targets -- -D warnings

      - name: Require committed lockfile
        run: git diff --exit-code -- Cargo.lock
