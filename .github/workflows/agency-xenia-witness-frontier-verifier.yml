name: Agency Xenia Witness Frontier Verifier

on:
  pull_request:
    paths:
      - 'crates/bridges/symthaea-xenia-authority/**'
      - 'crates/core/symthaea-authority/**'
      - 'docs/architecture/XENIA_WITNESS_FRONTIER_VERIFIER_V01.md'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'
      - '.github/workflows/agency-xenia-witness-frontier-verifier.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: agency-xenia-witness-frontier-verifier-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  source:
    name: Independent Xenia witness verification
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install pinned Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: clippy,rustfmt

      - name: Preserve checked-in Cargo.lock
        run: cp Cargo.lock "$RUNNER_TEMP/Cargo.lock.before"

      - name: Reconcile workspace metadata with Cargo
        run: cargo metadata --format-version 1 >/dev/null

      - name: Require additive local lock reconciliation only
        run: |
          python3 - <<'PY'
          import os, pathlib, sys, tomllib
          before = tomllib.loads((pathlib.Path(os.environ['RUNNER_TEMP']) / 'Cargo.lock.before').read_text())
          after = tomllib.loads(pathlib.Path('Cargo.lock').read_text())
          key = lambda p: (p['name'], p['version'], p.get('source'))
          b = {key(p): p for p in before.get('package', [])}
          a = {key(p): p for p in after.get('package', [])}
          removed = set(b) - set(a)
          changed = {k for k in b.keys() & a if b[k] != a[k]}
          added = set(a) - set(b)
          sourced = {k for k in added if k[2] is not None}
          if removed or changed or sourced:
              print('forbidden Cargo.lock reconciliation')
              print('removed:', sorted(removed, key=repr))
              print('changed:', sorted(changed, key=repr))
              print('new sourced:', sorted(sourced, key=repr))
              sys.exit(1)
          print('additive workspace/path nodes:', len(added))
          for item in sorted(added, key=repr):
              print(' +', item)
          PY

      - name: Record lock freshness
        run: |
          if cmp -s "$RUNNER_TEMP/Cargo.lock.before" Cargo.lock; then
            echo clean > "$RUNNER_TEMP/agency-lock-status"
          else
            echo stale > "$RUNNER_TEMP/agency-lock-status"
            git --no-pager diff -- Cargo.lock
          fi

      - name: Format Xenia verifier
        run: cargo fmt --check --package symthaea-xenia-authority

      - name: Test Xenia verifier
        run: cargo test --locked -p symthaea-xenia-authority

      - name: Clippy Xenia verifier
        run: cargo clippy --locked -p symthaea-xenia-authority --all-targets -- -D warnings

      - name: Record exact-head evidence
        if: always()
        run: |
          mkdir -p "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence"
          git rev-parse HEAD > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/HEAD"
          git rev-parse HEAD^{tree} > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/TREE"
          rustc -Vv > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/RUSTC.txt"
          cargo -V > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/CARGO.txt"
          sha256sum Cargo.lock > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/CARGO_LOCK_CANDIDATE_SHA256.txt"
          sha256sum crates/bridges/symthaea-xenia-authority/src/witness_frontier.rs > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/WITNESS_VERIFIER_SHA256.txt"
          sha256sum crates/bridges/symthaea-xenia-authority/tests/witness_frontier_interop.rs > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/WITNESS_INTEROP_VECTOR_SHA256.txt"
          sha256sum crates/bridges/symthaea-xenia-authority/tests/witness_frontier_verification.rs > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/WITNESS_CURRENTNESS_TEST_SHA256.txt"
          git --no-pager diff -- Cargo.lock > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/CARGO_LOCK_DIFF.patch"
          cat "$RUNNER_TEMP/agency-lock-status" > "$RUNNER_TEMP/agency-xenia-witness-frontier-evidence/CARGO_LOCK_STATUS.txt"

      - name: Upload source evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agency-xenia-witness-frontier-verifier-${{ github.sha }}
          path: ${{ runner.temp }}/agency-xenia-witness-frontier-evidence
          if-no-files-found: error
          retention-days: 14

      - name: Require checked-in Cargo.lock to be fresh
        run: test "$(cat "$RUNNER_TEMP/agency-lock-status")" = clean
