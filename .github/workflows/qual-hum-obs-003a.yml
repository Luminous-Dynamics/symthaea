name: QUAL HUM-OBS-003A exact subject

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: qual-hum-obs-003a-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  qualify:
    name: Exact-head HumanoidBench adapter qualification
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout exact candidate subject
        uses: actions/checkout@v4
        with:
          ref: cd9809c7aa72bdf349e2e629e9998016467bdad2
          fetch-depth: 1
      - name: Verify exact subject
        shell: bash
        run: |
          set -euo pipefail
          expected=cd9809c7aa72bdf349e2e629e9998016467bdad2
          actual="$(git rev-parse HEAD)"
          test "$actual" = "$expected"
          echo "qualified_subject=$actual"
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates/domains/symthaea-humanoid
      - name: Rustfmt exact humanoid crate
        run: cargo fmt --manifest-path crates/domains/symthaea-humanoid/Cargo.toml -- --check
      - name: Run HumanoidBench adapter source tests
        run: cargo test -p symthaea-humanoid humanoidbench_observatory --lib
      - name: Clippy humanoid library
        run: cargo clippy -p symthaea-humanoid --lib -- -D warnings
