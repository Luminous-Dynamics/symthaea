name: QUAL HUM-OBS-003D exact subject

on:
  pull_request:
    branches:
      - feat/hum-obs-003d-high-assurance-admission

permissions:
  contents: read

jobs:
  qualify:
    name: Exact-head HumanoidBench high-assurance qualification
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact frozen subject
        uses: actions/checkout@v4
        with:
          ref: e7eff33d3a5848838fa5008a738f97d53d4ad811
          fetch-depth: 1
      - name: Verify exact subject
        run: test "$(git rev-parse HEAD)" = "e7eff33d3a5848838fa5008a738f97d53d4ad811"
      - name: Install Rust 1.96.0
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.96.0
          components: rustfmt, clippy
      - name: rustfmt
        run: cargo fmt --manifest-path crates/domains/symthaea-humanoid/Cargo.toml -- --check
      - name: targeted tests
        run: cargo test -p symthaea-humanoid humanoidbench_high_assurance --lib
      - name: clippy
        run: cargo clippy -p symthaea-humanoid --lib -- -D warnings
