# Focused exact-head software-contract qualification for public capability-provenance transport.

name: Capability Provenance Transport Qualification

on:
  pull_request:
    paths:
      - '.github/workflows/capability-provenance-transport.yml'
      - 'scripts/qualify-capability-provenance-transport.sh'
      - 'crates/core/symthaea-continuity/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain*'
  workflow_dispatch:

concurrency:
  group: capability-provenance-transport-${{ github.event_name == 'workflow_dispatch' && github.run_id || github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  CHECKOUT_ACTION_SHA: 11d5960a326750d5838078e36cf38b85af677262
  UPLOAD_ARTIFACT_ACTION_SHA: ea165f8d65b6e75b540449e92b4886f43607fa02
  CAPABILITY_TRANSPORT_RUNNER_LABEL: ubuntu-24.04

jobs:
  capability-provenance-transport:
    name: Exact-head capability provenance transport
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout exact qualified head
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Verify pinned workflow actions
        shell: bash
        run: |
          set -euo pipefail
          test "$(grep -Fc "uses: actions/checkout@$CHECKOUT_ACTION_SHA" .github/workflows/capability-provenance-transport.yml)" -eq 1
          test "$(grep -Fc "uses: actions/upload-artifact@$UPLOAD_ARTIFACT_ACTION_SHA" .github/workflows/capability-provenance-transport.yml)" -eq 1
          if grep -Eq 'uses: actions/(checkout|upload-artifact)@v[0-9]+' .github/workflows/capability-provenance-transport.yml; then
            echo 'mutable major-version GitHub Action tag found' >&2
            exit 1
          fi

      - name: Install repository-pinned Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          channel="$(sed -nE 's/^[[:space:]]*channel[[:space:]]*=[[:space:]]*"([^"]+)".*/\1/p' rust-toolchain.toml | head -n1)"
          test -n "$channel"
          rustup toolchain install "$channel" --profile minimal --component rustfmt --no-self-update
          rustup override set "$channel"
          test "$(rustc --version | awk '{print $2}')" = "$channel"
          rustc --version
          cargo --version

      - name: Qualify public provenance transport
        env:
          QUALIFIED_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          CAPABILITY_TRANSPORT_RECEIPT: ${{ runner.temp }}/capability-provenance-transport-qualification-v1.tsv
        run: bash scripts/qualify-capability-provenance-transport.sh

      - name: Publish receipt digest
        if: always()
        shell: bash
        env:
          RECEIPT: ${{ runner.temp }}/capability-provenance-transport-qualification-v1.tsv
        run: |
          set -euo pipefail
          if [[ ! -f "$RECEIPT" ]]; then
            echo 'No transport qualification receipt was produced.'
            exit 0
          fi
          digest="$(sha256sum "$RECEIPT" | awk '{print $1}')"
          echo "capability-provenance-transport receipt_sha256=$digest"
          {
            echo
            echo "- receipt SHA-256: \`$digest\`"
            echo '- receipt bytes are not independently signed; the workflow log witnesses this digest.'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload qualification receipt
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: capability-provenance-transport-qualification-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/capability-provenance-transport-qualification-v1.tsv
          if-no-files-found: warn
          retention-days: 14
