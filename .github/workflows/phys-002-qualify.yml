name: PHYS-002 Bootstrap Qualifier

on:
  pull_request:
    paths:
      - 'crates/core/symthaea-physical-experiment/**'
      - '.github/workflows/phys-002-qualify.yml'
      - 'Cargo.lock'

permissions:
  contents: read

env:
  QUALIFY_SHA: ${{ github.event.pull_request.head.sha }}
  BASE_SHA: ${{ github.event.pull_request.base.sha }}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  qualify:
    name: Exact Head + Manifest Contract + Bootstrap Lock
    runs-on: ubuntu-slim
    timeout-minutes: 15
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.QUALIFY_SHA }}
          fetch-depth: 0

      - name: Bind exact head, one-commit topology, base, and scope
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$QUALIFY_SHA"
          git cat-file -e "$BASE_SHA^{commit}"
          test "$(git rev-parse HEAD^)" = "$BASE_SHA"
          test "$(git rev-list --count "$BASE_SHA..$QUALIFY_SHA")" -eq 1
          git diff --check "$BASE_SHA" "$QUALIFY_SHA"
          git diff --name-only "$BASE_SHA" "$QUALIFY_SHA" | tee /tmp/phys002-scope.txt
          unexpected="$(grep -Ev '^(Cargo\.lock|\.github/workflows/phys-002-qualify\.yml|crates/core/symthaea-physical-experiment/.*)$' /tmp/phys002-scope.txt || true)"
          test -z "$unexpected"
          printf 'qualification_sha=%s\nbase_sha=%s\n' "$QUALIFY_SHA" "$BASE_SHA" > /tmp/phys002-context.txt

      - name: Install Rust 1.96
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt, clippy

      - name: Prove toolchain
        run: |
          set -euo pipefail
          rustc -vV | tee -a /tmp/phys002-context.txt
          cargo -V | tee -a /tmp/phys002-context.txt
          rustc -vV | grep -q '^release: 1\.96\.0$'
          cargo -V | grep -q '^cargo 1\.96\.0 '

      - name: Materialize and audit cumulative bootstrap lock
        run: |
          set -euo pipefail
          mkdir -p /tmp/phys002-evidence
          cp Cargo.lock /tmp/phys002-evidence/Cargo.lock.before
          cargo metadata --format-version 1 >/tmp/phys002-evidence/workspace-metadata.json
          git diff -- Cargo.lock | tee /tmp/phys002-evidence/Cargo.lock.patch
          test -s /tmp/phys002-evidence/Cargo.lock.patch
          if grep -E '^-[^-]' /tmp/phys002-evidence/Cargo.lock.patch; then
            echo 'Cargo.lock bootstrap removed existing content; refusing broad churn.' >&2
            exit 1
          fi
          test "$(grep -c '^+\[\[package\]\]$' /tmp/phys002-evidence/Cargo.lock.patch)" -eq 2
          grep -q '^+name = "symthaea-physical-cognition"$' /tmp/phys002-evidence/Cargo.lock.patch
          grep -q '^+name = "symthaea-physical-experiment"$' /tmp/phys002-evidence/Cargo.lock.patch
          cp Cargo.lock /tmp/phys002-evidence/Cargo.lock.generated
          sha256sum Cargo.lock > /tmp/phys002-evidence/Cargo.lock.sha256
          grep -q '"name":"symthaea-physical-cognition"' /tmp/phys002-evidence/workspace-metadata.json
          grep -q '"name":"symthaea-physical-experiment"' /tmp/phys002-evidence/workspace-metadata.json

      - name: Focused format, check, tests, and clippy
        run: |
          set -euo pipefail
          rustfmt --edition 2024 --check crates/core/symthaea-physical-experiment/src/lib.rs
          cargo check -p symthaea-physical-experiment --locked
          cargo test -p symthaea-physical-experiment --locked
          cargo clippy -p symthaea-physical-experiment --all-targets --locked -- -D warnings

      - name: Postflight source immutability
        run: |
          set -euo pipefail
          git diff --exit-code -- . ':(exclude)Cargo.lock'
          cp /tmp/phys002-context.txt /tmp/phys002-evidence/context.txt
          cp /tmp/phys002-scope.txt /tmp/phys002-evidence/scope.txt

      - name: Upload bootstrap lock evidence
        uses: actions/upload-artifact@v4
        with:
          name: phys-002-bootstrap-lock
          path: /tmp/phys002-evidence
          if-no-files-found: error
          retention-days: 14
