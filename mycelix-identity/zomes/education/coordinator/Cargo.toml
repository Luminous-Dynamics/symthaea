[package]
name = "education_coordinator"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "Legacy Bridge: Academic credential coordinator with CSV import and DNS-DID verification"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
education_integrity = { path = "../integrity" }

[features]
default = []
