[package]
name = "verifiable_credential_integrity"
version = "0.1.0"
edition = "2021"
description = "W3C Verifiable Credentials integrity zome"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdi = { workspace = true }
holochain_serialized_bytes = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[features]
default = []
