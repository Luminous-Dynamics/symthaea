[package]
name = "credential_schema"
version = "0.1.0"
edition = "2021"
description = "Credential schema registry coordinator zome"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
credential_schema_integrity = { path = "../integrity" }
