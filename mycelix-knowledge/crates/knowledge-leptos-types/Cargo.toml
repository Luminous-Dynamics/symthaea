[package]
name = "knowledge-leptos-types"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "WASM-safe mirror types for Knowledge (serde only, no HDI)"

[dependencies]
serde = { version = "1", features = ["derive"] }
mycelix-claim-types = { path = "../../../crates/mycelix-claim-types" }

[workspace]
