[package]
name = "positioning"
version = "0.1.0"
edition = "2021"
description = "Stub for standalone builds (real crate in mycelix-position)"

[dependencies]
serde = { version = "1", features = ["derive"] }
