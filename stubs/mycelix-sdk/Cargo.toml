[package]
name = "mycelix-sdk"
version = "0.1.0"
edition = "2021"
description = "Stub crate for symthaea standalone builds"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[features]
default = []
standalone = []
holochain = []
