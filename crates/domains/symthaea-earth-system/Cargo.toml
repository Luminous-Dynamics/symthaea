[package]
name = "symthaea-earth-system"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Earth-system climate physics for Symthaea: zero-dimensional energy-balance model (Stefan-Boltzmann effective temperature, grey-atmosphere greenhouse, CO2 radiative forcing, climate sensitivity). Closed-form, ground-truth-checked."

# Zero external dependencies, no symthaea-core link: self-contained climate
# energy-balance physics. Builds fast and in isolation.
[dependencies]
