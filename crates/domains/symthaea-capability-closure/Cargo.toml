[package]
name = "symthaea-capability-closure"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Pure deterministic capability-dependency contracts for productive-closure analysis"

[lib]
path = "src/root.rs"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
