[package]
name = "symthaea-replicator-safety"
version = "0.1.0"
edition = "2024"
description = "Reference constitutional authority semantics for bounded replication safety; not production-admitted"
license = "AGPL-3.0-or-later"
publish = false
readme = "README.md"

[dependencies]

[dev-dependencies]
symthaea-replicator-semantics = { path = "../symthaea-replicator-semantics" }
sha2 = { workspace = true }
serde_json = { workspace = true }
hex = { workspace = true }
