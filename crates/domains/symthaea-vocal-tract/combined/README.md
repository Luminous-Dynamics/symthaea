# Symthaea vocal-tract Series 54–55 bundles

Series 54 starts from patched v53. Series 55 starts from patched v54.
Each patch archive contains five ordered `git am` patches, a README, and
patch-level SHA-256 checksums. The patched snapshots and deliverable checksums
are included beside them.
