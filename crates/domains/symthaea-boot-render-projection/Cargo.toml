[package]
name = "symthaea-boot-render-projection"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Truth-preserving projection from Spore semantic boot frames into exact renderer timeline segments"

[dependencies]
symthaea-boot-presentation = { path = "../symthaea-boot-presentation" }
