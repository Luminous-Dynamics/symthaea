[package]
name = "symthaea-game-theory"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Formal game-theory layer for Symthaea: validated N-player/deviation analysis plus independent mixed-strategy beliefs, exact best response, and one-step logit choice. Pure-std, zero dependencies."

# Pure-std, zero dependencies, no symthaea-core link.
[dependencies]
