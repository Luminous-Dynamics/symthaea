# Applying Symthaea Music Theory Patch Series 34

1. Verify the exact base: the exact implemented and qualified Series 33 final tree.
2. Verify prerequisite manifests, source trees, and evidence packages.
3. Apply patches in `PATCH_ORDER.md` without manual edits.
4. Compare the resulting tree with the authored final-tree identity.
5. Execute every native, independent, transactional, privacy, and reproduction lane.
6. Rebuild public artifacts byte-for-byte.
7. Record failures, blockers, and unexecuted gates.

Do not convert a plan, authorization, dry run, or trigger report into a committed-state claim.
