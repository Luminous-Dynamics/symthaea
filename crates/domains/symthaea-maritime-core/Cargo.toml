[package]
name = "symthaea-maritime-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Non-weaponized maritime autonomy primitives for Symthaea"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
