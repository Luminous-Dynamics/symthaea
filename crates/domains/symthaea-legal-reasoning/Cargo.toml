[package]
name = "symthaea-legal-reasoning"
version = "0.4.0"
edition = "2024"
rust-version = "1.85"
license = "AGPL-3.0-or-later"
description = "Deterministic legal-reasoning microkernel with bounded recursive inference, proof graphs, legal time, lifecycle, and canonical evidence"
keywords = ["legal-reasoning", "deontic", "defeasible", "hohfeld", "logic"]
categories = ["algorithms", "data-structures"]

# Pure std, zero dependencies, and no symthaea-core link. Natural-language
# interpretation, precedent analogy, and normative policy remain outside the
# trusted formal kernel.
[dependencies]
