[package]
name = "symthaea-boot-protocol"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Typed, privacy-preserving boot observability protocol for Symthaea"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
