[serve]
address = "0.0.0.0"
# Dev/test slot (PORTS.md 8400-8409, "ad-hoc, never persistent"). 8400 is
# muse_studio (the axum API this app talks to); 8401 is symthaea-ui.
# Not yet a permanent allocation.
port = 8402

[build]
target = "index.html"
