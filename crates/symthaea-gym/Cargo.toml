[package]
name = "symthaea-gym"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
petgraph = "0.6"       # The interaction graph
nalgebra = "0.33"      # Eigenvalue decomposition (Spectral K)
serde = { version = "1.0", features = ["derive"] }
