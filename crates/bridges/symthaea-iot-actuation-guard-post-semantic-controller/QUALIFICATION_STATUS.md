# Qualification Status

Candidate only. Do not describe this crate as compiler-qualified or promotion-ready until the exact-head `IoT Actuation Guard Post-Semantic Controller` workflow completes successfully.

The qualification claim is limited to:

- challenge construction only from opaque `PersistedSemanticAcceptance`;
- guard-owned nonce/time and expiry clipped by both device-reality expiry and original effect deadline;
- controller binding to the exact semantic head, transport head, physical envelope, and whole signed device-appraisal object;
- no second device-attestation object in the controller response;
- canonical bounded report/evidence parsing and raw-evidence commitment checks; and
- no controller trust decision, final permit, JIT lease, HAL, network, process-exec, or unsafe surface.

Current controller-key trust and interlock policy remain a later fixed guard stage.
