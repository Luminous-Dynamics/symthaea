[package]
name = "symthaea-algorithm-evidence-collector"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Read-only experiment provenance collector for Symthaea algorithm discovery"

[dependencies]
serde = { workspace = true, features = ["derive"] }
symthaea-algorithms = { path = "../../core/symthaea-algorithms" }
thiserror = { workspace = true }
