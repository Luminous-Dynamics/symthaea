[package]
name = "symthaea-kubernetes-endpointslice-bridge"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Read-only Kubernetes EndpointSlice topology augmenter for the Symthaea integration fabric"

[dependencies]
symthaea-integration-core = { path = "../../core/symthaea-integration-core" }
symthaea-kubernetes-bridge = { path = "../symthaea-kubernetes-bridge" }
serde_json = { workspace = true }
blake3 = { workspace = true }
