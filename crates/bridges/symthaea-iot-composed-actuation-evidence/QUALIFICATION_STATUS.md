# Qualification status

Status: **candidate / not yet compiler-qualified**.

Required exact-head evidence:

- composition consumes `RevalidatedXeniaTransport`, `PersistedSemanticAcceptance` and `VerifiedPostSemanticPhysicalInterlock` by value;
- durable admission and semantic heads are re-derived from their retained checkpoint objects;
- independently verified transport and durable branches converge on the exact physical envelope, signed receipt commitment and transport-trust generation;
- admission-bound device reality converges on the durable reservation/configuration and exact signed appraisal object;
- the post-semantic controller challenge converges on the admission head, physical envelope, transport generation, semantic head, device appraisal, semantic persistence time and effect/reality deadlines;
- the controller statement converges on the exact challenge, appraisal, device, envelope, semantic head and transport generation;
- statement/evidence/response commitments reproduce the opaque interlock proof;
- the composition commitment includes transport exact-evidence/key lineage, durable heads, device-reality trust/policy/key, controller trust/policy/key and relevant deadlines;
- `ComposedActuationEvidence` remains non-clone and non-serializable;
- production contains no current registry/fence, relying-party wall/monotonic clock, final permit, reusable JIT/HAL lease, network/process execution or unsafe surface;
- the full real regression independently verifies the same exact portable Xenia evidence into two affine branches before composition;
- an independently valid but different transport lineage is rejected;
- Rust 1.94 check/tests/package-local strict Clippy and Rust 1.96 formatting pass on the exact head;
- no unreviewed external sourced dependency drift is introduced.

Local-only workspace `Cargo.lock` bookkeeping remains to be intentionally frozen before promotion. No live-current, actuator-I/O or physical-effect claim is made by this crate.
