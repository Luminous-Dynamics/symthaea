# Post-Semantic Qualification Status

Candidate only. Do not describe `VerifiedPostSemanticPhysicalInterlock` as compiler-qualified or promotion-ready until the exact-head `IoT Actuation Guard Post-Semantic Interlock` workflow completes successfully.

The qualification claim is limited to the corrected sibling path:

- exact post-semantic challenge/report correlation;
- guard-local challenge/report freshness;
- exact anchored `PhysicalInterlockPolicyV1`;
- exact anchored anti-rollback controller-trust head;
- trust-generation anti-retroactivity;
- fixed RFC 8032 Ed25519 verification of the **new statement digest**;
- real full-chain positive, wrong-key, revoked-key, and interlock-set regressions; and
- no conversion to legacy `VerifiedPhysicalInterlock`, final permit, JIT lease, or HAL authority.

Historical `verify_ingress()` remains a separate lineage and is not redefined by this qualification.
