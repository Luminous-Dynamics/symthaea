[package]
name = "mycelix-cluster-manifest"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Cluster manifest types for the Mycelix modular architecture"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
