[package]
name = "symthaea-zkproof-core"
version = "0.1.0"
edition = "2021"
description = "Shared types for Symthaea ZK proofs (guest + host)"

[features]
default = ["std"]
std = ["serde/std"]

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
