[package]
name = "mycelix-claim-types"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Unified epistemic classification types — shared between Prism browser and Knowledge cluster"

[dependencies]
serde = { version = "1", features = ["derive"] }

[workspace]
