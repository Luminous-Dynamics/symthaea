[package]
name = "symthaea-interface-types"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Transport-neutral runtime/session/event types for Symthaea interfaces"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
