[package]
name = "symthaea-app-resolution"
version = "0.1.0"
edition = "2024"
description = "Deterministic fail-closed application identity resolution over the Symthaea migration corpus"
license = "AGPL-3.0-or-later"

[dependencies]
symthaea-app-db = { path = "../symthaea-app-db" }
