[package]
name = "symthaea-trust-kernel"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Lifecycle-governed trust, quorum clock, and continuity primitives for Symthaea authority systems"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
