[package]
name = "supplychain-leptos-types"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "WASM-safe mirror types for Supply Chain (serde only, no HDI)"

[dependencies]
serde = { version = "1", features = ["derive"] }

[workspace]
