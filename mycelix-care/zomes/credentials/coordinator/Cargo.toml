[package]
name = "credentials"
version = "0.1.0"
edition = "2021"
description = "Care credentials coordinator zome - credential issuance and verification"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
credentials_integrity = { path = "../integrity" }
