[package]
name = "gamification_coordinator"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Gamification coordinator zome - implements XP, badges, streaks, and rewards logic"

[lib]
crate-type = ["cdylib", "rlib"]
name = "gamification_coordinator"

[dependencies]
# Holochain Development Kit (HDK) for zome functions
hdk.workspace = true

# Link to the integrity zome
gamification_integrity = { path = "../integrity" }

# Core types
praxis-core = { path = "../../../crates/praxis-core" }

# Serialization
serde.workspace = true
serde_json.workspace = true

# Cross-cluster bridge (consciousness gating)
mycelix-bridge-common = { path = "../../../../crates/mycelix-bridge-common" }
