[package]
name = "srs_coordinator"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Spaced Repetition System coordinator zome - implements SM-2 algorithm business logic"

[lib]
crate-type = ["cdylib", "rlib"]
name = "srs_coordinator"

[dependencies]
# Holochain Development Kit (HDK) for zome functions
hdk.workspace = true

# Link to the integrity zome
srs_integrity = { path = "../integrity" }

# Core types
praxis-core = { path = "../../../crates/praxis-core" }

# Serialization
serde.workspace = true
serde_json.workspace = true
