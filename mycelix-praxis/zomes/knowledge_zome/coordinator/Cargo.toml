[package]
name = "knowledge_coordinator"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Knowledge Roots coordinator zome - implements decentralized curriculum graph logic"

[lib]
crate-type = ["cdylib", "rlib"]
name = "knowledge_coordinator"

[dependencies]
# Holochain Development Kit (HDK) for zome functions
hdk.workspace = true

# Link to the integrity zome
knowledge_integrity = { path = "../integrity" }

# Core types
praxis-core = { path = "../../../crates/praxis-core" }

# Serialization
serde.workspace = true
serde_json.workspace = true
