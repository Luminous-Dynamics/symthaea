apiVersion: v1
kind: Service
metadata:
  name: {{ include "mycelix.fullname" . }}-api
  labels:
    {{- include "mycelix.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mycelix.api.selectorLabels" . | nindent 4 }}
