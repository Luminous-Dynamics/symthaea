apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app.kubernetes.io/name: mycelix-web
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: mycelix-web
