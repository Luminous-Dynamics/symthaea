[package]
name = "gradient-quality"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "2.3.2", default-features = false, features = ["std"] }
zk-gradient-core = { path = "../../core" }
