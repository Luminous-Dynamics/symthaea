[package]
name = "care_bridge"
version = "0.1.0"
edition = "2024"
description = "Care bridge coordinator zome - cross-hApp queries and events"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
care_bridge_integrity = { path = "../integrity" }
