[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    slow: marks tests as slow (proof generation tests)

# Default: run only fast tests
addopts = -m "not slow" -v

# For full test suite including slow tests:
# pytest -m "" tests/
