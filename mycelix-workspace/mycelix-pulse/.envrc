# Mycelix-Mail direnv configuration
# This file automatically loads the Nix flake environment

if has nix; then
  use flake
else
  echo "Warning: Nix not found. Install Nix for the full development environment."
  echo "See: https://nixos.org/download.html"
fi

# Load local .env if it exists
dotenv_if_exists .env
