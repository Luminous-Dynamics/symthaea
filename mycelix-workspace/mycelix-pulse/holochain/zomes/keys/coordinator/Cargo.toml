[package]
name = "keys"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = "0.6"
serde = { version = "1.0", features = ["derive"] }
keys_integrity = { path = "../integrity" }
