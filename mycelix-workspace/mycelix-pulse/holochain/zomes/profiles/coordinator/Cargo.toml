[package]
name = "mail_profiles"
version = "0.1.0"
edition = "2021"
description = "Coordinator zome for Mycelix Mail Profiles"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = "0.6.0"
serde = { version = "1.0", features = ["derive"] }
mail_profiles_integrity = { path = "../integrity" }
