# Mycelix Domain Module Template
#
# Use this template to create a new domain module for Mycelix Sensorium.
#
# Steps:
# 1. Copy this directory to mycelix-sensorium/crates/domain-yourdomain/
# 2. Rename the package and struct below
# 3. Implement the DomainModule trait methods
# 4. Add as a feature flag to mycelix-sensorium/Cargo.toml
# 5. Add a `mod` + match arm in sensorium-shell pages/mod.rs (optional — for inline UI)
#
# For external frontends (community extensions), you don't need to compile
# into the Sensorium. Instead, create an extensions.json manifest and build
# your frontend as a standalone web app using @mycelix/sdk or mycelix-leptos-client.

[package]
name = "domain-template"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Template for creating a Mycelix domain module"

[dependencies]
sensorium-domain-trait = { path = "../mycelix-sensorium/crates/sensorium-domain-trait" }
