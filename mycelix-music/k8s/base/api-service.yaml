apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app.kubernetes.io/name: mycelix-api
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3100
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: mycelix-api
