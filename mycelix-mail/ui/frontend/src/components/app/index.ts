/**
 * App Components Index
 *
 * Main application shell and integration:
 * - App shell with navigation
 * - Provider wrappers
 * - Route integration
 */

export {
  default as EpistemicApp,
  EpistemicAppShell,
  EpistemicProviders,
} from './EpistemicApp';
