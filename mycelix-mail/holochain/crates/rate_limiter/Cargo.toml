[package]
name = "mycelix_rate_limiter"
version = "0.1.0"
edition = "2021"
description = "Rate limiting utilities for Mycelix Mail zomes"
license = "AGPL-3.0-or-later"

[dependencies]
hdk = { workspace = true }
holochain_serialized_bytes = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = "1.0"

[lib]
crate-type = ["rlib"]
